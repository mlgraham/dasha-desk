{
  "name": "ocm",
  "private": true,
  "type": "module",
  "description": "Open-Compute Marketplace \u2014 gateway, ledger and host agent.",
  "scripts": {
    "start": "node gateway/server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check:secrets": "node --test tests/no-secrets.test.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "pg": "^8.13.1",
    "@aws-sdk/client-sesv2": "^3.700.0"
  }
}
